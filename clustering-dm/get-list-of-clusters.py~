import sys, screed
import collections

#get list of cluster ids to grab
list = []
for line in open(sys.argv[1]):
    line = int(line.rstrip())
    list.append(line)

last_key = ''

d = {}

for n, line in enumerate(open(sys.argv[2])):
    if line.startswith('>'):
        cluster = int(line.rstrip().split(' ')[1])
            last_key 

    else:
        id = line.split('>')[1].split('_')[0]
        if d.has_key(last_key):
            d[last_key].append(line)            
        else:
            d[last_key] = [line]

    print d

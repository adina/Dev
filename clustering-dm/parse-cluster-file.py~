import sys, screed

last_key = ''

for line in open(sys.argv[1]):
    if line.startswith('>'):
        cluster = int(line.rstrip().split(' ')[1])
        last_key = cluster
    else:
        if d.has_key(last_key):
            d[last_key] = 
